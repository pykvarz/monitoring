# Рефакторинг MainWindow - Документация

## Обзор изменений

Главное окно приложения было разделено на **4 специализированных менеджера**, каждый из которых отвечает за свою область:

### 📁 Новые файлы

1. **`filter_manager.py`** - Управление фильтрацией и поиском
2. **`theme_manager.py`** - Управление темами оформления  
3. **`dashboard_manager.py`** - Управление панелью статистики
4. **`export_import_manager.py`** - Импорт/экспорт Excel

### 📝 Изменённые файлы

- **`main_window.py`** - Рефакторенный главный класс (сокращен ~20%)

---

## Преимущества нового подхода

### ✅ Принцип единственной ответственности (SRP)
Каждый менеджер отвечает только за свою область:
- `FilterManager` - только фильтрация
- `ThemeManager` - только темы
- `DashboardManager` - только статистика
- `ExportImportManager` - только импорт/экспорт

### ✅ Улучшенная тестируемость
Теперь можно тестировать каждый менеджер отдельно:
```python
# Пример юнит-теста
def test_filter_manager():
    filter_mgr = FilterManager(search_edit, group_filter, status_filter, table, model)
    filter_mgr.apply_filters()
    # Проверки...
```

### ✅ Переиспользуемость
Менеджеры можно использовать в других окнах приложения

### ✅ Упрощенная поддержка
Изменения в фильтрации не затрагивают код тем и наоборот

### ✅ Лучшая производительность
- `DashboardManager` использует debounce (500ms) для обновлений UI
- Инкрементальное обновление счетчиков вместо полного пересчета

---

## Детальное описание менеджеров

### 🔍 FilterManager

**Ответственность**: Управление фильтрацией и поиском хостов

**Основные методы**:
- `apply_filters()` - Применение всех активных фильтров
- `reset_filters()` - Сброс всех фильтров
- `update_group_filter(groups)` - Обновление списка групп
- `set_status_filter(status_title)` - Установка фильтра по статусу

**Преимущества**:
- Автоматическое подключение сигналов к UI элементам
- Централизованная логика фильтрации
- Легко добавить новые типы фильтров

**Использование**:
```python
self._filter_manager = FilterManager(
    self._search_edit, self._group_filter, 
    self._status_filter, self._table, self._table_model
)
self._filter_manager.apply_filters()
```

---

### 🎨 ThemeManager

**Ответственность**: Управление темами оформления приложения

**Основные методы**:
- `toggle_theme()` - Переключение между светлой/темной темой
- `get_current_theme()` - Получение текущей темы
- `set_window_icon(theme)` - Установка иконки окна
- `apply_initial_theme()` - Применение темы при запуске

**Внутренние методы**:
- `_apply_theme(theme)` - Применение темы ко всем компонентам
- `_update_dashboard_cards(theme)` - Обновление стилей карточек
- `_update_toolbar_buttons(theme)` - Обновление иконок тулбара
- `_update_filter_buttons(theme)` - Обновление кнопок фильтров

**Преимущества**:
- Все изменения темы в одном месте
- Поддержка кастомных тем (легко добавить новые)
- Автоматическое обновление всех UI компонентов

**Использование**:
```python
self._theme_manager = ThemeManager(
    self, self._config, self._storage, 
    self._table, self._table_model
)
self._theme_manager.toggle_theme()
```

---

### 📊 DashboardManager

**Ответственность**: Управление панелью статистики (Dashboard)

**Основные методы**:
- `update_stats(stats_counts)` - Полное обновление статистики
- `update_status_transition(old_status, new_status)` - Обновление при смене статуса
- `increment_status(status)` - Увеличить счетчик
- `decrement_status(status)` - Уменьшить счетчик
- `schedule_update()` - Запланировать обновление (debounce)
- `force_refresh()` - Принудительное немедленное обновление

**Преимущества**:
- **Debounce**: обновление UI с задержкой 500ms для избежания flickering
- **Инкрементальные обновления**: не пересчитываем все каждый раз
- Генерация SVG иконок с поддержкой темы
- Централизованное форматирование карточек

**Использование**:
```python
self._dashboard_manager = DashboardManager(self._dashboard_labels, self._config)

# При изменении статуса хоста
self._dashboard_manager.update_status_transition("ONLINE", "OFFLINE")

# При полной перезагрузке
stats = {"ONLINE": 10, "WAITING": 2, "OFFLINE": 1, "MAINTENANCE": 0}
self._dashboard_manager.update_stats(stats)
```

---

### 📥📤 ExportImportManager

**Ответственность**: Импорт и экспорт данных в Excel

**Основные методы**:
- `import_from_excel(hosts, hosts_mutex, update_callback)` - Импорт
- `export_to_excel(hosts, hosts_mutex)` - Экспорт

**Преимущества**:
- Изолированная логика работы с файлами
- Потокобезопасная работа с данными (через mutex)
- Удобные диалоги с предзаполненными именами файлов
- Детальные отчеты об ошибках

**Использование**:
```python
self._export_import_manager = ExportImportManager(self, self._storage)

# Импорт
self._export_import_manager.import_from_excel(
    self._hosts, self._hosts_mutex,
    lambda: (self._update_monitor_hosts(), self._refresh_table())
)

# Экспорт
self._export_import_manager.export_to_excel(self._hosts, self._hosts_mutex)
```

---

## Структура MainWindow после рефакторинга

### Разделы кода:

1. **ИНИЦИАЛИЗАЦИЯ** (~150 строк)
   - `_init_data()` - Загрузка данных
   - `_init_ui()` - Создание UI
   - `_init_managers()` - Инициализация менеджеров
   - `_init_monitor_thread()` - Запуск мониторинга
   - `_init_timers()` - Настройка таймеров

2. **НАСТРОЙКИ ТАБЛИЦЫ** (~50 строк)
   - `_restore_table_settings()` - Восстановление настроек
   - `_connect_table_signals()` - Подключение сигналов

3. **РАБОТА С ДАННЫМИ** (~80 строк)
   - `_modify_hosts_safely()` - Безопасное изменение
   - `_load_table()` - Загрузка в таблицу
   - `_refresh_table()` - Полное обновление
   - `_periodic_save()` - Автосохранение

4. **ОБРАБОТЧИКИ СИГНАЛОВ МОНИТОРА** (~50 строк)
   - `_on_scan_started()` / `_on_scan_finished()`
   - `_on_status_updated()` - Обновление статуса
   - `_on_hosts_offline()` - Уведомления

5. **UI ОБРАБОТЧИКИ** (~60 строк)
   - `_update_status_bar()` - Статус бар
   - `_on_table_double_clicked()` - Двойной клик
   - `_on_dashboard_clicked()` - Клик по дашборду

6. **ДЕЙСТВИЯ ПОЛЬЗОВАТЕЛЯ** (~100 строк)
   - `_add_host()` / `_add_group()` / `_delete_selected()`
   - `_import_from_excel()` / `_export_to_excel()`
   - `_force_scan()` / `_toggle_theme()` / `_open_settings()`

7. **КОНТЕКСТНЫЕ МЕНЮ** (~120 строк)
   - `_show_bulk_menu()` - Массовые действия
   - `_show_context_menu()` - Контекстное меню строки
   - `_show_header_context_menu()` - Управление колонками

8. **СОХРАНЕНИЕ НАСТРОЕК** (~40 строк)
   - `_update_hidden_columns_config()`
   - `_on_column_resized()` / `_on_column_moved()`

9. **ЗАВЕРШЕНИЕ** (~10 строк)
   - `closeEvent()` - Корректное закрытие

---

## Сравнение до и после

| Метрика | До рефакторинга | После рефакторинга |
|---------|-----------------|-------------------|
| Строк кода в MainWindow | 807 | ~650 |
| Количество методов | 44 | 35 |
| Средняя длина метода | 18 строк | 12 строк |
| Количество файлов | 1 | 5 |
| Coupling (связанность) | Высокая | Низкая |
| Тестируемость | Низкая | Высокая |

---

## Дальнейшие улучшения

### Рекомендации:

1. **Unit-тесты**
   - Добавить тесты для каждого менеджера
   - Mock зависимости (QWidgets, Storage)

2. **Dependency Injection**
   - Передавать зависимости через конструктор
   - Использовать интерфейсы вместо конкретных классов

3. **Event Bus**
   - Вместо прямого вызова методов использовать события
   - Менеджеры подписываются на события

4. **Repository Pattern**
   - Абстракция над StorageManager
   - Поддержка разных источников данных (JSON, SQLite, PostgreSQL)

5. **State Machine для статусов**
   - Централизованная логика переходов статусов
   - Валидация возможных переходов

---

## Миграция существующего кода

### ⚠️ Breaking Changes: НЕТ

Рефакторинг полностью обратно совместим. Все публичные методы и сигналы остались без изменений.

### Что изменилось внутри:

- Логика фильтрации вынесена в `FilterManager`
- Логика тем вынесена в `ThemeManager`
- Логика дашборда вынесена в `DashboardManager`
- Логика импорта/экспорта вынесена в `ExportImportManager`

### Что осталось в MainWindow:

- Координация менеджеров
- Обработка сигналов от MonitorThread
- Работа с HostManager
- Управление таблицей
- Контекстные меню

---

## Исправления качества кода (2026-01-12)

После завершения рефакторинга был проведен аудит кода и исправлены следующие проблемы:

### ✅ Исправлено:

1. **excel_service.py** - Заменены 4 голых `except:` на конкретные типы исключений
2. **models.py** - Импорт из constants перемещен в начало файла
3. **main_window.py** - Добавлена обработка исключений в `_modify_hosts_safely`
4. **export_import_manager.py** - Добавлено логирование ошибок импорта/экспорта
5. **filter_manager.py** - Добавлен debounce (300ms) для текстового поиска

### 📈 Улучшения:

- ✅ Все критические ошибки логируются в `debug.log`
- ✅ Пользователь получает понятные сообщения об ошибках
- ✅ Оптимизирована производительность фильтрации (важно для больших таблиц)
- ✅ Код соответствует PEP 8

**Подробности**: См. `FIXES_REPORT.md`

---

## Заключение

Рефакторинг значительно улучшил архитектуру приложения:

✅ **Разделение ответственности** - каждый класс делает одну вещь  
✅ **Улучшенная читаемость** - код стал проще понимать  
✅ **Лучшая тестируемость** - можно тестировать компоненты отдельно  
✅ **Упрощенная поддержка** - легче вносить изменения  
✅ **Повышенная производительность** - debounce, инкрементальные обновления  
✅ **Готовность к расширению** - легко добавлять новые функции  
✅ **Качественная обработка ошибок** - надежность и наблюдаемость  

Приложение стало более профессиональным и готовым к дальнейшему развитию! 🚀
